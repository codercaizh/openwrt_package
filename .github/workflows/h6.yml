---
name: Openwrt Build

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - master

  workflow_dispatch:

jobs:
  build:
    name: Build H6 Openwrt
    runs-on: ubuntu-latest
    steps:
      - name: Update system
        run: |
          export test=123
          echo $test
          echo $test2
        env: 
          test2: 666
