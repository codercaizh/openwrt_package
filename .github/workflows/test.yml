name: test
on:
  workflow_dispatch: 
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          ref: dev
      - run: |
          ls
          echo "#${date}" >> version.sh
          cat version.sh
          git config user.name github-robot
          git config user.email 545347837@qq.com
          git add .
          git commit -m "auto generated"
          git push
 
