name: test
on:
  workflow_dispatch: 
jobs:
  job1:
    uses: codercaizh/openwrt_package/.github/workflows/test_sub.yml@master
  job2:
    needs: job1
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ needs.job1.outputs.oo1 }} ${{ needs.job1.outputs.oo2 }}
