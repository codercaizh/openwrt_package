FROM ubuntu:22.10
MAINTAINER codercaizh <545347837@qq.com>
RUN export DEBIAN_FRONTEND=noninteractive && apt-get update && apt-get install -y mkisofs fdisk btrfs-progs parted uuid-runtime dosfstools build-essential asciidoc binutils bzip2 gawk gettext git jq npm libncurses5-dev libz-dev patch python3.10 python2.7 unzip zlib1g-dev lib32gcc-s1 libc6-dev-i386 subversion flex uglifyjs git-core gcc-multilib p7zip p7zip-full msmtp libssl-dev texinfo libglib2.0-dev xmlto qemu-utils upx libelf-dev autoconf automake libtool autopoint device-tree-compiler g++-multilib antlr3 gperf wget curl swig rsync && apt-get autoremove --purge && apt-get clean
ENV RUN_SCRIPT_PATH=/opt/run.sh
RUN echo 'echo -e "The Openwrt compilation environment is ready!\n\nYou can enter the container and perform compilation operations by executing the command "docker exec -it $ID /bin/bash".\n\nAlternatively, you can mount the compilation script to /opt/run.sh and then restart the container. The container will automatically run the script.😁" && /usr/bin/tail -f /dev/null' > /opt/run.sh
ENTRYPOINT /bin/bash $RUN_SCRIPT_PATH